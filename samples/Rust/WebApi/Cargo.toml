[package]
name = "webapi"
version = "1.0.0"
edition = "2021"

[dependencies]
actix-web = "4"
mongodb = { version = "2", features = ["openssl-tls"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
